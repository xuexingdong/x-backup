spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      username: xuexingdong
      password: 123456
      label: 1.0.0
    service-registry:
      auto-registration:
        fail-fast: true
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka